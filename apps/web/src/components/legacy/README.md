# Legacy Components

This directory contains retained components that have no active imports:

- `GoalPathBuilder.tsx`
- `Toolbar.tsx`

They remain available for a deliberate migration or removal decision. New
production code must use the feature directories documented in
`docs/architecture/directory-structure.md` instead.
